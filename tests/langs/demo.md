```javascript
const renderDemo = (to, how) => ({ quistion: how, aciton: to });
const str = '123' + 123;
const isComplite = str.length >= 6;
```
```lua
local ts_utils, ok = pcall(require, 'nvim-treesitter.ts_utils')
```
```html
<div data-parent="plugin" class="swap" id="_sw">How to swap attributes</div>
```
```vue
<template>
  <app-item :key="line.id" v-for="line in lines">
    {{ line.content }}
  </app-item>
</template>
<script setup>
  const lines = [
    { content: 'how', id: 1 },
    { id: 2, content: 'to' },
  ];
</script>
```
