console.log(one, two);
console.log(two, one);

const obj = { one: 'one', two: 'two' };
const obj = { two: 'two', one: 'one' };

const arr = [ 1, 2 ];
const arr = [ 2, 1 ];

function toObject(arg1, arg2) {
  return {
    arg1,
    arg2,
  };
};

function toObject(arg2, arg1) {
  return {
    arg1,
    arg2,
  };
};

console.log(getSibling(), elseif, another, or, node, oneName)
console.log({ another, oneName, node, elseif, or, one = getSibling() })

const more = 123 > 122;
const more = 122 > 123;

const more = 123 > 122;
const more = 122 < 123;

const one = 1;

console.log(123);

console.log(
  one,
  two
);
console.log(
  two,
  one
);

const obj = {
  one: 'one',
  two: 'two',
};
const obj = {
  two: 'two',
  one: 'one',
};
